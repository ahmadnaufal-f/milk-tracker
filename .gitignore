# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.*
!.env.example

# Firebase
.firebase/
firebase-debug.log*
firestore.indexes.json
# (Note: firestore.indexes.json is often ignored if generated dynamically, but good to keep if manually managed. 
# actually, keep indexes tracked usually. I'll just add .firebase/ and logs)

# Testing
coverage/

# OS
Thumbs.db
